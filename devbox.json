{
  "packages": [
    "nodejs@20",
    "git@latest"
  ],
  "shell": {
    "init_hook": [
      "echo 'vscode-git-graph devbox environment loaded (Node 20 + pnpm via corepack)'"
    ],
    "scripts": {
      "build": "pnpm compile",
      "test": "pnpm test",
      "package": "pnpm package"
    }
  }
}
